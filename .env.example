# Copy this file to .env and fill in your values.
# .env is gitignored — never commit it.

# ── OpenClaw version to use (update here to track upstream) ──────────────────
OPENCLAW_VERSION=latest
NEKAISE_VERSION=1.0.0

# ── Gateway port (host side) ────────────────────────────────────────────────
GATEWAY_PORT=20629
