# Copy this file to .env and fill in your secrets.
# .env is gitignored — never commit it.

# ── OpenClaw version to use (update here to track upstream) ──────────────────
OPENCLAW_VERSION=2026.2.21-2
NEKAISE_VERSION=1.0.0

# ── Model ─────────────────────────────────────────────────────────────────────
OPENCLAW_MODEL=openai-codex/gpt-5.3-codex

# ── Channel tokens (leave blank to disable a channel) ─────────────────────────
TELEGRAM_BOT_TOKEN=
SLACK_BOT_TOKEN=
SLACK_APP_TOKEN=
DISCORD_BOT_TOKEN=

# ── Gateway ───────────────────────────────────────────────────────────────────
GATEWAY_PORT=18789
GATEWAY_TOKEN=

# ── Local data directory ──────────────────────────────────────────────────────
# The ./data/ directory is mounted into the container.
# It contains openclaw.json (generated on first run), workspace, logs, memory.
# Back it up if you want to preserve user workspace customizations.
