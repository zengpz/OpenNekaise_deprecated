# Secrets — never commit
.env

# Runtime state (generated by Docker)
/.opennekaise/runtime/

# Runtime memory logs
/.opennekaise/runtime/memory/

# User building data (keep sample buildings tracked)
# To ignore your own buildings, add them here or use a local .git/info/exclude


# OS / editor
.DS_Store
*.swp
